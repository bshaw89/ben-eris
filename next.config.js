/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
      domains: ['i.imgur.com', 'https://ben-eris.vercel.app/'],
    }
  }
  
  module.exports = nextConfig